# Button

Gold momentum CTA (`primary`) is the default. Use `secondary` for blue authority actions, `outline` / `ghost` for quieter chrome.

Do: one primary gold button per view. Don't: invent a third brand color or use gold for every action.
