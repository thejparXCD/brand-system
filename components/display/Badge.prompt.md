**Badge** — small status / category pill, mono type.

```jsx
<Badge tone="gold" dot>Live</Badge>
<Badge tone="success">On track</Badge>
<Badge tone="brand" subtle={false}>Mission</Badge>
```

Tones: `neutral` · `brand` · `gold` · `success` · `warning` · `danger`. `subtle={false}` for solid fill.
