**Card** — surface container. Use `tone="ink"` for dark brand panels, `interactive` for clickable cards (adds hover lift).

```jsx
<Card tone="light" elevation="md">…</Card>
<Card tone="ink" interactive>…</Card>
```

Tones: `light` · `sunken` · `ink`. Elevation: `none` · `sm` · `md` · `lg`.
