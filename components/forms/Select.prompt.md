**Select** — styled native dropdown matching `Input`. Pass `options` as strings or `{value,label}`.

```jsx
<Select label="Service line" placeholder="Choose one" options={[
  'Technology & Infrastructure', 'Growth & Workforce', 'Mission Strategy & Readiness'
]} />
```

Props mirror Input: `label`, `helperText`, `error`, `disabled`.
