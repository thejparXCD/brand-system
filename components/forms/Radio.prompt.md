**Radio** — single option; group with a shared `name`.

```jsx
<Radio name="plan" value="audit" label="Reality Audit" defaultChecked />
<Radio name="plan" value="system" label="Momentum System" />
```
