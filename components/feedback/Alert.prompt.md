**Alert** — inline message banner; tone sets color, accent rule, and default icon.

```jsx
<Alert tone="success" title="Audit complete">We surfaced 14 hidden signals.</Alert>
<Alert tone="warning" onClose={dismiss}>Two service lines need attention.</Alert>
```

Tones: `info` · `success` · `warning` · `danger`.
