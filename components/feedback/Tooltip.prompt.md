**Tooltip** — hover/focus hint on a navy chip; wraps one trigger.

```jsx
<Tooltip label="Surfaced from machine signals" placement="top">
  <IconButton aria-label="Info"><Icon name="info" /></IconButton>
</Tooltip>
```
