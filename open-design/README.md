# Open Design package (mirror)

Canonical package: **`design-systems/celebration-onpoint/`**

This folder mirrors that package for backwards compatibility. Prefer importing `design-systems/celebration-onpoint` in Open Design.

See root `OPEN-DESIGN.md` for import instructions.
